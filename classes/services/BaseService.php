<?php

/**
 * Base service providing common utilities to all services (e.g. repositories access).
 * Services encapsulate business rules, orchestration and validations.
 */
class BaseService {
    // future common helpers could be added here (transactions, logging, etc.)
}
