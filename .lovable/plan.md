

# إضافة بيانات العملاء لمؤسسة Vogatchi Travel

## الوضع الحالي
- مؤسسة **Vogatchi Travel** موجودة بالـ ID: `c069d1ca-3aa7-4dfa-9ca3-87b183e5273d`
- حالياً **0 عملاء** مسجلين لها
- الملف المرفوع يحتوي على بيانات عملاء لكن `organization_id = null`

## الخطة

### خطوة واحدة: إدراج البيانات مع تعيين organization_id

1. قراءة ملف الـ SQL المرفوع
2. استبدال كل `organization_id = null` بـ `organization_id = 'c069d1ca-...'` (Vogatchi Travel)
3. تنفيذ الـ INSERT عبر أداة الإدراج في قاعدة البيانات

### ملاحظات تقنية
- البيانات ستُربط تلقائياً بمؤسسة Vogatchi فقط
- سياسات RLS ستضمن ظهورها لأعضاء المؤسسة فقط
- لن يتم تعديل أي هيكل في قاعدة البيانات — فقط إدراج بيانات

